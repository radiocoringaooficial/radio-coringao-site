{
  "installCommand": "npm install --include=dev && npx prisma generate && npx prisma migrate deploy",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/api/admin/(.*)", "destination": "/api/admin" },
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
